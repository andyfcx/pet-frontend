# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['biometeo-front.py'],
    pathex=[],
    binaries=[('.venv/lib/python3.12/site-packages/biometeo/*.so', 'biometeo')],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='biometeo-front',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
app = BUNDLE(
    exe,
    name='biometeo-front.app',
    icon=None,
    bundle_identifier=None,
)
